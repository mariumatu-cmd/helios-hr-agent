"""Operational scripts.

This is a package only so that the pure decision functions inside these scripts
(for example `assert_health.assess`) can be imported and unit-tested. The
scripts remain runnable directly as `python scripts/<name>.py`.
"""
